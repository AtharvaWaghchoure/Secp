[package]
name = "gen_test_vectors"
version = "0.1.0"
edition = "2021"

[dependencies]
# k256 = secp256k1 in Rust. "ecdsa" enables signing; "sha2" for the prehash helper.
k256 = { version = "0.13", features = ["ecdsa"] }
hex = "0.4"
